apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: front-end-claim0
  name: front-end-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
