apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: back-end-claim0
  name: back-end-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
